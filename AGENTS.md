see `PLAN.md` if you forget our plan in each phase, or have done something that needs to be documented there.

Use path aliases (`@/...`) for all imports from `src/`.

Use modern Tailwind CSS classes for styling.

Do not try to workaround technical limitations unless you are asked to. If something can't be done neatly, report back and provide suggestions.

Always check if there are compilation errors after your changes.